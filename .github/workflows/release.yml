name: Reusable Release

on:
  workflow_call:
    inputs:
      image:
        description: Docker image repository name
        required: true
        type: string
      event_name:
        description: Original caller event name (push or workflow_dispatch)
        required: true
        type: string
      bump:
        description: Manual bump input (patch, minor, major)
        required: false
        type: string
      tool_ref:
        description: shipkit ref for go run module@ref
        required: false
        default: main
        type: string
      go_version:
        description: Go version for running tool (leave empty to auto-detect from go.mod)
        required: false
        default: ""
        type: string
    secrets:
      DOCKERHUB_USERNAME:
        required: false
      DOCKERHUB_TOKEN:
        required: false
      HOMEBREW_TAP_GITHUB_TOKEN:
        required: false

permissions:
  contents: write
  pull-requests: read

jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      dryrun: ${{ steps.plan.outputs.dryrun }}
      next_tag: ${{ steps.plan.outputs.next_tag }}
      goreleaser_docker: ${{ steps.plan.outputs.goreleaser_docker }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ inputs.go_version }}
          go-version-file: ${{ inputs.go_version == '' && 'go.mod' || '' }}
          cache: true

      - name: Plan release
        id: plan
        run: >-
          go run github.com/karloie/shipkit/cmd/shipkit@${{ inputs.tool_ref }} plan
          -mode=release
          -bump=${{ inputs.bump }}
          -image=${{ inputs.image }}
          -sha=${{ github.sha }}
          -owner=${{ github.repository_owner }}
          -repo=${{ github.event.repository.name }}
          -run-id=${{ github.run_id }}
        env:
          GITHUB_EVENT_NAME: ${{ inputs.event_name }}
          GITHUB_TOKEN: ${{ github.token }}
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
          HOMEBREW_TAP_GITHUB_TOKEN: ${{ secrets.HOMEBREW_TAP_GITHUB_TOKEN }}

      - uses: goreleaser/goreleaser-action@v6
        if: ${{ steps.plan.outputs.dryrun != 'true' }}
        with:
          distribution: goreleaser
          version: "~> v2"
          args: release --clean ${{ steps.plan.outputs.goreleaser_yml_current != 'true' && format('--config={0}', steps.plan.outputs.goreleaser_yml_new) || '' }}
        env:
          GITHUB_TOKEN: ${{ github.token }}
          HOMEBREW_TAP_GITHUB_TOKEN: ${{ secrets.HOMEBREW_TAP_GITHUB_TOKEN }}

      - name: Cleanup on failure
        if: ${{ failure() && steps.plan.outputs.next_tag != '' && steps.plan.outputs.dryrun != 'true' }}
        run: >-
          go run github.com/karloie/shipkit/cmd/shipkit@${{ inputs.tool_ref }} git-tag-cleanup
          -tag=${{ steps.plan.outputs.next_tag }}

  docker:
    needs: release
    if: ${{ needs.release.outputs.dryrun != 'true' && needs.release.outputs.goreleaser_docker != 'true' }}
    uses: ./.github/workflows/docker.yml
    with:
      image: ${{ inputs.image }}
      event_name: push
      tag: ${{ needs.release.outputs.next_tag }}
      tool_ref: ${{ inputs.tool_ref }}
      go_version: ${{ inputs.go_version }}
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
